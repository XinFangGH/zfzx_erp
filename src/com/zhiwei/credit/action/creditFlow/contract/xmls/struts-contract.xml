<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">
<struts>
    <package name="contract" extends="struts-default" namespace="/contract">
		<action name="*ProcreditContract" class="ProcreditContractAction" method="{1}">
			<result>${successResultValue}</result>
		</action>
		<action name="*ProduceHelper" class="ProduceHelperAction" method="{1}">
			<result>${successResultValue}</result>
		</action>
		<action name="*DocumentTemplet" class="DocumentTempletAction" method="{1}">
			<result>${successResultValue}</result>
		</action>
		<action name="*CsElementCategory" class="CsElementCategoryAction" method="{1}">
			<result>${successResultValue}</result>
		</action>
		<!-- @HT_version3.0 合同改造新增 -->
		<action name="*ContractHelper" class="ContractHelperAction" method="{1}">
			<result>${successResultValue}</result>
		</action>
	</package>
</struts>